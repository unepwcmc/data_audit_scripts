<%= form_for(@importance_level) do |f| %>
  <% if @importance_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@importance_level.errors.count, "error") %> prohibited this importance_level from being saved:</h2>

      <ul>
      <% @importance_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
