<%= form_for(@dataset_scope) do |f| %>
  <% if @dataset_scope.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dataset_scope.errors.count, "error") %> prohibited this dataset_scope from being saved:</h2>

      <ul>
      <% @dataset_scope.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
