<%= form_for(@drife) do |f| %>
  <% if @drife.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drife.errors.count, "error") %> prohibited this drife from being saved:</h2>

      <ul>
      <% @drife.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
