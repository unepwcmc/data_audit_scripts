<%= form_for(@use_level) do |f| %>
  <% if @use_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@use_level.errors.count, "error") %> prohibited this use_level from being saved:</h2>

      <ul>
      <% @use_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
